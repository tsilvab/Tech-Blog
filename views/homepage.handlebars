{{#each posts as |post| }}
    <article class="post">
        {{> post-info post}}
        {{else}}
        <h6 class="pb-4 has-text-centered is-italic">There is currently no posts</h6>
        </article>
    {{/each}}